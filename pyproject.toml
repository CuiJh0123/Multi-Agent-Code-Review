[project]
name = "python-code-review"
version = "0.1.0"
description = "OpenAI-compatible code review pipeline migrated from Java demo."
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
